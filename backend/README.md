# Image Upload Assignment - Backend

# Day 1 Progress

- Started with professor's Example2 backend code.
- Added `.gitignore` to ignore node_modules.
- Installed backend dependencies: `multer` and `cors`.
- Implemented `/api/upload` route using Multer.
- Kept the original `/api/getImage` search feature working.
- Tested backend server on http://localhost:3000
